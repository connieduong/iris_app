<div class="col-sm-6">
<h3>Recent Posts</h3>
<div class="s">
  <% @posts.each do |p| %>
  <%= link_to p.title, post_path(p) %>
  <ul class="">
      <%= p.body %>
      <br>
      <br>

        <%= link_to(p.user.name, user_path(p.user)) %>
      <br>
      <br>

  </ul>
  <% end %>
</div>

</div>
